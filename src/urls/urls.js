const baseURL = 'https://loremflickr.com/600/600';

const urls = {
    girls: `${baseURL}/girls`,
    cats: `${baseURL}/cats`,
    mountains: `${baseURL}/mountains`,
    flowers: `${baseURL}/flowers`,
    food: `${baseURL}/food`
}

export {baseURL, urls};